[test]
preload = ["./tests/setup.ts"]
timeout = 10000

[bundle]
# Keep modules external
external = []

[install]
registry = "https://registry.npmjs.org"

[install.scopes]
"@ninots" = { token = "$NPM_CONFIG_TOKEN", url = "https://registry.npmjs.org/" }
